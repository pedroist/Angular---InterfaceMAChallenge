import { HousesFilterValueRoomsPipe } from './houses-filter-value-rooms.pipe';

describe('HousesFilterValueRoomsPipe', () => {
  it('create an instance', () => {
    const pipe = new HousesFilterValueRoomsPipe();
    expect(pipe).toBeTruthy();
  });
});
