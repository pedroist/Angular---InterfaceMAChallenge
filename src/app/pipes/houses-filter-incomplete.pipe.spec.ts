import { HousesFilterIncompletePipe } from './houses-filter-incomplete.pipe';

describe('HousesFilterIncompletePipe', () => {
  it('create an instance', () => {
    const pipe = new HousesFilterIncompletePipe();
    expect(pipe).toBeTruthy();
  });
});
