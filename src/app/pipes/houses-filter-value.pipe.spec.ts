import { HousesFilterValuePipe } from './houses-filter-value.pipe';

describe('HousesFilterValuePipe', () => {
  it('create an instance', () => {
    const pipe = new HousesFilterValuePipe();
    expect(pipe).toBeTruthy();
  });
});
